/* ============================================================
   SEARCH RESULTS PAGE  —  shown at  /search
   ------------------------------------------------------------
   Not built yet. This placeholder exists so the search bar has
   somewhere to go and the folder structure is visible.

   When built, sections will be:
     1. FILTER BAR   — price, rooms, amenities
     2. RESULTS GRID — matching listings
     3. MAP PANEL    — pins beside the results
   ============================================================ */

export default function SearchPage() {
  return (
    <div className="page-container" style={{ paddingBlock: "var(--space-2xl)" }}>
      <h1 className="listing-grid__title">Search results</h1>
      <p style={{ color: "var(--color-text-muted)" }}>
        This page is not built yet.
      </p>
    </div>
  );
}
