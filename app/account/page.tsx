/* ============================================================
   ACCOUNT PAGE  —  shown at  /account
   Styles: styles/pages/account.css
   ============================================================ */

import AccountHomeSection from "@/components/account/AccountHomeSection";

export default function AccountPage() {
  return (
    /* ACCOUNT HUB — every setting, one tile each */
    <AccountHomeSection />
  );
}
