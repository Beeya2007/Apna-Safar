/* ============================================================
   PAYMENTS PAGE  —  shown at  /account/payments
   Styles: styles/pages/account.css
   ============================================================ */

import PaymentsSection from "@/components/account/PaymentsSection";

export default function PaymentsPage() {
  return (
    /* PAYMENTS — saved cards and every receipt */
    <PaymentsSection />
  );
}
