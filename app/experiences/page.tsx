/* ============================================================
   EXPERIENCES PAGE  —  shown at  /experiences
   Styles: styles/pages/browse.css
   ============================================================ */

import ExperiencesSection from "@/components/browse/ExperiencesSection";

export default function ExperiencesPage() {
  return (
    /* EXPERIENCES — everything bookable that is not a bed */
    <ExperiencesSection />
  );
}
