/* ============================================================
   BECOME A HOST PAGE  —  shown at  /host
   Styles: styles/pages/host.css
   ============================================================ */

import HostPitchSection from "@/components/host/HostPitchSection";

export default function HostPage() {
  return (
    /* HOST PITCH — the case for listing, and the three steps */
    <HostPitchSection />
  );
}
