/* ============================================================
   HOST EARNINGS PAGE  —  shown at  /host/earnings
   Styles: styles/pages/host.css
   ============================================================ */

import EarningsSection from "@/components/host/EarningsSection";

export default function HostEarningsPage() {
  return (
    /* EARNINGS — totals, the next payout, and every past one */
    <EarningsSection />
  );
}
