/* ============================================================
   HOST LISTINGS PAGE  —  shown at  /host/listings
   Styles: styles/pages/host.css
   ============================================================ */

import ListingsSection from "@/components/host/ListingsSection";

export default function HostListingsPage() {
  return (
    /* YOUR PLACES — every listing this host owns */
    <ListingsSection />
  );
}
