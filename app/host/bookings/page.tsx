/* ============================================================
   HOST BOOKINGS PAGE  —  shown at  /host/bookings
   Styles: styles/pages/host.css
   ============================================================ */

import HostBookingsSection from "@/components/host/HostBookingsSection";

export default function HostBookingsPage() {
  return (
    /* BOOKINGS — every stay booked against your places */
    <HostBookingsSection />
  );
}
