/* ============================================================
   HOST DASHBOARD PAGE  —  shown at  /host/dashboard
   Styles: styles/pages/host.css
   ============================================================ */

import DashboardSection from "@/components/host/DashboardSection";

export default function HostDashboardPage() {
  return (
    /* DASHBOARD — attention, earnings, places, upcoming stays */
    <DashboardSection />
  );
}
